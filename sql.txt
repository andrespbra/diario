
-- SCRIPT DE ESTRUTURA COMPLETA (DIÁRIO DE BORDO)
-- Copie e cole tudo no SQL Editor do Supabase e clique em 'Run'

-- 1. Tabela de Perfis de Usuários
CREATE TABLE IF NOT EXISTS public.user_profiles (
    id UUID REFERENCES auth.users ON DELETE CASCADE PRIMARY KEY,
    name TEXT NOT NULL,
    username TEXT UNIQUE NOT NULL,
    nivel TEXT DEFAULT 'Analista',
    must_change_password BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 2. Tabela de Atendimentos (Tickets)
CREATE TABLE IF NOT EXISTS public.tickets (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id UUID REFERENCES auth.users ON DELETE SET NULL,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    
    -- Informações do Cliente/Local
    customer_name TEXT,
    location_name TEXT,
    task_id TEXT,
    service_request TEXT,
    hostname TEXT,
    n_serie TEXT,
    subject TEXT,
    
    -- Detalhes do Suporte
    analyst_name TEXT,
    support_start_time TEXT,
    support_end_time TEXT,
    description TEXT,
    analyst_action TEXT,
    
    -- Validadores (Checkboxes)
    is_due_call BOOLEAN DEFAULT FALSE,
    used_acfs BOOLEAN DEFAULT FALSE,
    has_ink_staining BOOLEAN DEFAULT FALSE,
    part_replaced BOOLEAN DEFAULT FALSE,
    part_description TEXT,
    tag_vldd BOOLEAN DEFAULT FALSE,
    tag_nlvdd BOOLEAN DEFAULT FALSE,
    
    -- Validação Técnica (N2/Tiger)
    test_with_card BOOLEAN DEFAULT FALSE,
    sic_withdrawal BOOLEAN DEFAULT FALSE,
    sic_deposit BOOLEAN DEFAULT FALSE,
    sic_sensors BOOLEAN DEFAULT FALSE,
    sic_smart_power BOOLEAN DEFAULT FALSE,
    
    -- Testemunha/Responsável
    client_witness_name TEXT,
    client_witness_id TEXT,
    
    -- Status e Prioridade
    status TEXT DEFAULT 'Aberto',
    priority TEXT DEFAULT 'Média',
    is_escalated BOOLEAN DEFAULT FALSE,
    is_tiger_team BOOLEAN DEFAULT FALSE,
    
    -- IA e Validação Final
    ai_suggested_solution TEXT,
    validated_by TEXT,
    validated_at TIMESTAMPTZ
);

-- 3. Habilitar RLS (Segurança)
ALTER TABLE public.user_profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.tickets ENABLE ROW LEVEL SECURITY;

-- 4. Políticas de Segurança (Simplificadas para evitar recursão)
DROP POLICY IF EXISTS "allow_all_read_profiles" ON public.user_profiles;
CREATE POLICY "allow_all_read_profiles" ON public.user_profiles FOR SELECT TO authenticated USING (true);

DROP POLICY IF EXISTS "allow_all_read_tickets" ON public.tickets;
CREATE POLICY "allow_all_read_tickets" ON public.tickets FOR SELECT TO authenticated USING (true);

DROP POLICY IF EXISTS "allow_auth_insert_tickets" ON public.tickets;
CREATE POLICY "allow_auth_insert_tickets" ON public.tickets FOR INSERT TO authenticated WITH CHECK (auth.uid() = user_id);

DROP POLICY IF EXISTS "allow_auth_update_tickets" ON public.tickets;
CREATE POLICY "allow_auth_update_tickets" ON public.tickets FOR UPDATE TO authenticated USING (true);

-- 5. Trigger para criar perfil automático ao registrar no Auth
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS trigger AS $$
BEGIN
  INSERT INTO public.user_profiles (id, name, username, nivel, must_change_password)
  VALUES (
    new.id, 
    COALESCE(new.raw_user_meta_data->>'name', split_part(new.email, '@', 1)), 
    COALESCE(new.raw_user_meta_data->>'username', split_part(new.email, '@', 1)), 
    COALESCE(new.raw_user_meta_data->>'nivel', 'Analista'), 
    TRUE
  )
  ON CONFLICT (id) DO NOTHING;
  RETURN new;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE PROCEDURE public.handle_new_user();
